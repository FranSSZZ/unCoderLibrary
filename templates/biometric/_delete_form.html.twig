<form method="post" action="{{ path('app_biometric_delete', {'id': biometric.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ biometric.id) }}">
    <button class="btn">Delete</button>
</form>
